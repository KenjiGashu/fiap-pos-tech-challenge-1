name: CI/CD - Sistema Mecânica

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:

  build-test:

    runs-on: ubuntu-latest

    env:
      FIAP_POS_SECRET: ${{ secrets.FIAP_POS_SECRET }}
      FIAP_POS_SALT: ${{ secrets.FIAP_POS_SALT }}
      FIAP_POS_EMAIL: ${{ secrets.FIAP_POS_EMAIL }}
      FIAP_POS_APP_PASSWORD: ${{ secrets.FIAP_POS_APP_PASSWORD }}

    services:

      postgres:
        image: postgres:16-alpine

        env:
          POSTGRES_DB: mydb
          POSTGRES_USER: myuser
          POSTGRES_PASSWORD: mypassword

        ports:
          - 5432:5432

        options: >-
          --health-cmd="pg_isready -U myuser -d mydb"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Run Tests
        run: dotnet test --configuration Release --no-build

  docker:

    needs: build-test

    runs-on: ubuntu-latest

    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/sistema-mecanica:latest

  # deploy:

  #   needs: docker

  #   runs-on: self-hosted

  #   steps:

  #     - uses: actions/checkout@v4

  #     - name: Terraform Init
  #       working-directory: terraform
  #       run: terraform init

  #     - name: Terraform Validate
  #       working-directory: terraform
  #       run: terraform validate

  #     - name: Terraform Plan
  #       working-directory: terraform
  #       run: terraform plan

  #     - name: Terraform Apply
  #       working-directory: terraform
  #       run: terraform apply -auto-approve

  #     - name: Apply Kubernetes Manifests
  #       run: kubectl apply -k k8s/

  #     - name: Wait Deployment
  #       run: |
  #         kubectl rollout status deployment/sistema-mecanica \
  #           -n gashu-app \
  #           --timeout=120s

  #     - name: Show Resources
  #       run: |
  #         kubectl get all -n gashu-app

  #     - name: Show Pods
  #       run: |
  #         kubectl get pods -n gashu-app

  #     - name: Show Services
  #       run: |
  #         kubectl get svc -n gashu-app
